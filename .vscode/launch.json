{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run All Tasks",
            "type": "java",
            "request": "launch",
            "cwd": "${workspaceFolder}/Server",
            "preLaunchTask": "Run All Tasks",
            "console": "integratedTerminal"
        }
    ]
}
